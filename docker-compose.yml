services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - pm-data:/app/data
    env_file:
      - .env
    environment:
      PM_DATABASE_PATH: /app/data/pm.db
    init: true

volumes:
  pm-data:
